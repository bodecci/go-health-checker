# A health-check written in Go
##### this program checks to see if an domain is available or not. To run this, pass the domain flag: ```go run . --domain <domain_name>```
###### e.g ```go run . --domain google.com```